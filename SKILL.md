---
name: rising-loop
description: "リリース済みサービスの運営・改善ループを回す進行管理スキル。実データ（アクセス解析・DB・売上等）を見て診断し、ボトルネックを洗い出し、そこから施策を選んで打ち手を実行し、施策ごとの現在評価を計測のたびに更新する——これを複数の目標について並行して回す。現在地と施策評価は各プロジェクトの `loops/LXX.html`（`LOOP_DATA`）、出来事は `loops/logs/*.md` に持ち、一覧の `loops/index.html` から見る。ユーザーが「運営どうなってる」「数字を見て」「ユーザー動向は」「改善ループ」「ボトルネック」「施策を打ちたい」「効果を測りたい」「KPI」「rising-loop」「ライジング・ループ」「loop-manager」「ループを回して」「リリース後どう」等と言ったときに必ず使う。明示的に頼まれなくても、**リリース済みのサービスについて数字の話が出たら積極的に発動してよい**。除外: これから作る機能の進行管理は、そのための進行管理スキルがあればそちらへ渡す。"
user-invokable: true
---

# rising-loop — 運営・改善ループ

数字を見て、詰まっているところを見つけ、手を打ち、経過を見て、施策ごとに効いたかどうかの現在評価を更新する。これを終わりなく回す。

## 7つの原理

**以下の規約は、すべてこの7つから導かれている。** 迷ったとき、規約に書いていない場面に出たときは、ここに戻って考える。

1. **正は一つ。置き場所は役割で決まる** — 同じことを2箇所に書かない。二重に持つと必ずズレて、**ズレたことに気づかない**。ループでは **ループ頁の `LOOP_DATA` が正。散文の数字はその写し**。**頁の中の写し（`trials[]`⇄TRIAL・`records[]`⇄評価・`summary`⇄本文）と殻の一覧は残る二重。消さずに、気づける形に畳んである**
2. **数字は「いつ・どの窓・何の」とセットでしか意味を持たない** — 裸の数字は、古いのか新しいのか、何と比べたのかが分からないまま判断に使われる
3. **決めるのはユーザー。AI は選択肢を出して止まる** — 「やる」は着手の合図であって、実装の承認ではない
4. **予想を先に書く。後知恵で判定しない** — 数字を見てから「効いた」と言うのは、いくらでもできてしまう
5. **終わっていないものを、終わったことにしない** — 実施が終わったことと、効果が確かめられたことは別
6. **読む人の負荷に上限を置く** — 来月これを読むのは本人。並ぶほど読まなくなる
7. **規則はスキルに、値はループに** — 規則をループに書くと、ループを増やすたびに全部へ同じことを書く羽目になる

なぜその規約になったのか（実際に起きた失敗）は `references/なぜこの規約か.md`。

## 何を扱うスキルか

**すでに動いているもの**を扱う。これから作るものは扱わない。

| | 作る側の進行管理 | rising-loop |
|---|---|---|
| 対象 | **これから作るもの** | **すでに動いているもの** |
| 終わり | 完成したら閉じる | **終わらない** |
| 反応 | すぐ返る（テストが通る／通らない） | **数日〜数週遅れて返る**（ユーザーの行動） |

作る話が出てきたら、そのための進行管理の仕組みがあればそちらへ渡す（原理1）。**両方に同じことを書かない。**

---

## 最初にやること

### ★ 新しく作るときは、先に道具を確認する

**画面の右ペイン（AIチャット）は必須。** 画面のボタンはすべて「文言をコピーするだけ」の作りなので、
**貼る先がその場に無いと往復が成立しない**。作りはじめる前に、必ずこれを調べる。

```bash
command -v ttyd && ttyd --version              #=== ペインの中身。1.7 以上が要る
command -v claude || ls ~/.local/bin/claude    #=== 相談相手そのもの（PATH に居ないことがある）
command -v codex   #=== codex で使うなら（任意。無くても止めない）
```

- **入っていなければ、そこで止めてユーザーに入れてもらう。** macOS なら `brew install ttyd`。
  **勝手にインストールしない**（環境を変える操作なので、頼まれるまでやらない）
- 入ったのを確認してから続ける
- ★ **「無いから右ペイン抜きで作る」を選ばない。** 中核が欠けた画面ができあがる。
  しかも **CSS だけ残って markup と JS が落ちる**という壊れ方をするので、見た目では気づけない
- どうしても入らない環境で省くときは、**その旨を `loops/README.md` に1行書く。**
  次に触る人が「入れ忘れた」と誤解しないようにする
- ★ **ttyd の起動はユーザーのターミナルで。AI は起動コマンドを見せるだけで、自分で起動しない**（Claude Code の中から起動すると会話が保存されなくなる。理由は `references/HTML生成.md`）

**`loops/` を作るときの画面の作り方**: 殻は `assets/index.html`、ループ頁は `assets/loop.html` から作り、`rising.css` `rising.js` は `loops/` にそのままコピーする。殻の CONST ブロック（`PROJECT_DIR` `SERVICE_NAME` `PANES`）を埋める。**タイトル下の主要な数字（`.rev`）は必ず置く**。何を置くかは `references/HTML生成.md`「主要な数字」の決め方で AI が候補を1つ出し、確認を取ってから。詳しくは `references/HTML生成.md`。

### ★ 作り終えたら、開ける URL を渡して、そこから始めてもらう

**作っただけで終わらない。** 導入してループを作ったら、**クリックできる URL** を渡し、
**以降はその画面が入口になる**ことを伝える。

```
file:///Users/…/プロジェクト/loops/index.html
```

- ⚠️ **裸のファイルパスを貼らない。** そのままではクリックできない。**必ず先頭に `file://` を付ける**
  （パスにスペースがあるときだけ `%20` に替える）
- 「**まずこれを開いてください。次からはこの画面から始めます**」と一言添える
- 使い方は画面の `? 使い方` にある（初回は自動で開く）ので、チャットで長く説明しない
- ★ **締めは必ずこの一言で終える**: 「**まず施策を1つ選んで、その行の『指示する』から『TRIAL に移す』と送ってください**」。
  施策案を並べて終わると、**何をやっていいか分からなくなる**（★ 実際に言われた 2026-09-16）。
  番号を打っても始まらない。始まるのは「指示する」の文言を右に貼ったときだけ
- 右ペインは、画面の `💡` から**コマンドをコピーして自分のターミナルで実行**してもらう
  （`sh …/loops/chat-pane.sh claude` または `… codex`）。AI は実行しない
- ★ **ファイルのパスを渡さない。** 見る場所は画面だけ（原理1）
- 右ペインが真っ黒なときの直し方は画面の中に書いてある（`💡 15秒たってもチャットが出ない？`）ので、
  **チャットの返答で手順を説明し直さない**

### ★ 旧形式の `loops/` は、最初に移行を提案する

2026-09-11 より前に作った `loops/` は、右ペインの仕組みが古く**そのままでは繋がらない**。
`loops/` を見つけたら、作業に入る前にこれを確かめる。

```bash
grep -l 'var SESS' loops/index.html; ls loops/chat-pane.sh; ls loops/rising.js
```

- `loops/rising.js` が無ければ **1.2.x の単一ファイル形式**（下の 1.3.0 の移行）
- `var SESS` があり `loops/chat-pane.sh` が無ければ**旧形式**。「右ペインの仕組みが新しくなっています。先に移行しますか」と1問だけ聞く
- 移行の中身は3つ。**子エージェントに出す**（手順の詳細は `references/HTML生成.md` の右ペイン節）
  1. スキルの `assets/chat-pane.sh` を `loops/chat-pane.sh` に置く
  2. 画面の右ペイン部分を新しい形にする。**1.3.0 の移行（下）を先に実行すれば、殻が `assets/index.html` に入れ替わるのでこれは済む。** `PROJECT_DIR` と `PANES`（CONST ブロック）を埋め直すことだけ確認する
  3. 旧 `SESS` に入っていたセッションIDを `loops/.chat-sessions` に `<画面ID> claude <ID>` で書き移す（会話を捨てない）
- 終わったら、ユーザーのターミナルで `sh …/loops/chat-pane.sh claude` を立て直してもらう。AI は起動しない
- ★ **「rising-loop が X.Y.Z になったので、このループを合わせて」と言われたら、この節を実行する**（install.sh が更新後にこの文言を案内している）。数字の更新（「更新」）とは別
  - **最初に `/rising-loop` を呼び出して SKILL.md を読み直す。** このスレッドが更新前から続いていれば、古い規約が文脈に残っている。読み直しで上書きする
  - 次に `loops/index.html` の使い方の帯にある版（`v1.2.3` など）を見る。**スキルの `VERSION` と同じなら「読み直しました。合わせ済みです」と1行で終える。** 違うときだけ以下をやる
  - やることは①ループ頁の形式を最新に ②共通ファイルと殻を入れ替える ③下の版ごとの移行。終わったら何を変えたかを3行で報告する
  - ★ ②の中身: **共通ファイル `rising.css` `rising.js` `chat-pane.sh` `update/common.py`（`assets/loopdata.py` の写し）を `assets/` のものでそのまま上書き**（値を埋めない）。**殻は `python3 ~/.claude/skills/rising-loop/assets/shell-update.py loops/`**（CONST ブロックと LOOPS ブロックだけを引き継いで入れ替え、共通ファイル4つも同時に上書きする。`loops/update/` が無ければ作る。**ループごとの `update/LXX.py` は触らない**）。**ループ頁 `LXX.html` は触らない**——**構造版 `data-page-schema` が上がる版（1.5.0 など）だけ**、その版の移行手順で直す（違っていれば `shell-update.py` が警告を出す）
  - ★ `shell-update.py` は各ループ頁の**古いところ**も一覧で出す（決まった文言が無い・共通部品が無い・どの CSS にも定義が無いクラス）。版が離れていると、共通ファイルは上書きされるのに頁の markup だけ古いまま残る。**スクリプトは直さない。** 出たものをユーザーに見せ、直すかを聞いてから直す（原理3）
  - 触らないもの: ループ固有の中身（各頁の `LOOP_DATA` と本文・ID・`PROJECT_DIR`・`PANES`・各 `LXX.html`）
- **1.3.0（画面の分割）**: `loops/rising.js` が無ければ 1.2.x の単一ファイル形式。**移行を提案して止まる。勝手に実行しない**（原理3）
  1. `python3 ~/.claude/skills/rising-loop/assets/split-index.py loops/ --dry-run` を実行し、検算の表（どの頁に何が入るか・数字が落ちていないか）をユーザーに見せる
  2. ユーザーの「はい」で、`--dry-run` なしで実行する。元の `index.html` は `loops/.tmp/index-before-split.html` に退避される
  3. **分割で作る頁は schema 1**（`LOOP_DATA` は `hist` と `funnel` だけ）。続けて **1.5.0 の移行（下）**を走らせて schema 2 にする。分割だけだと `LXX.md` が残ったままになる
- **1.5.0（`LXX.md` の廃止。ループ頁の `LOOP_DATA` が正になる）**: **判定は頁に `data-page-schema="2"` が無ければ 1.4.x 以前**（`LXX.md` の有無で判定しない。1.2.x にも md はある）。**移行を提案して止まる。勝手に実行しない**（原理3）
  1. `python3 ~/.claude/skills/rising-loop/assets/merge-md.py loops/ L01 --dry-run` を実行し、表（何が `LOOP_DATA` に入るか・md の数字のうち頁に無いもの）をユーザーに見せる
  2. ユーザーの「はい」で `--dry-run` なしを実行する。**1ループ＝1子で並列してよい**（子は1メッセージでまとめて起動する）
  3. 続けて子が、判断の要るぶんをやる
     - ★ **子に渡す入力は3つだけ**: ①そのループの頁のパス（`loops/LXX.html`）②スクリプトの報告 `loops/.tmp/LXX-merge-report.md` ③退避した md のコピー `loops/.tmp/LXX.md`
     - スクリプトが出した「頁に無い数字」を仕分ける。**生の値**は `hist.points[].note` か `days[]` へ、**派生値（割合・倍率）は捨ててよい**。仕分けの結果を報告させる
     - **md と html が食い違うときは和集合。** md にしか無い注記は `points[].note` に足し、html にしか無い一文は残す。**同じ点の値は、スクリプトが常に md を採っている。頁側が新しいと分かる点は子が戻す**（頁に `updated` が無いので機械では決められない）。md に無い項目（評価日など）は `null`
     - `records[]` を md 本文の RECORD の見出しから1件ずつ写す（実施日・要旨・狙い・実施前の数字と比較条件・予想と予想日・判定日・評価と評価日・確定判定）
     - ★ **子の報告の形**（4行）: ①足した点 ②捨てた派生値 ③統合した note ④写した `records[]` の件数
     - ★ **子への禁止**: 担当ループ以外の頁と `index.html` を開かない。`LXX.md` を消さない（退避はスクリプトが済ませている）
  4. ★ **順序**: 子が `records[]` まで写し終えてから、**「更新」を1回**走らせて散文・表・ファネルを `LOOP_DATA` に揃える（移行だけだと頁の中で日付がずれる。途中で挟むと2回走らせることになる）
  5. `LXX.md` の `loops/.tmp/` への退避はスクリプトが済ませている。揃ったのを確認したら、元の `LXX.md` の**削除はユーザーがやる。AI は消さない**
- **1.6.0（`loops/update/` — ループごとの更新スクリプト）**: **移行は不要。** `update/LXX.py` が無いループは「更新」の初回扱いで、子が更新の最後に書き残す（「数字を更新し、施策を再評価する」手順 B）。`update/common.py` は「合わせて」の `shell-update.py` が置く
- **1.1.0 より前の `loops/`**（`trial:` が1件の形／TRIAL と RECORD に同じ施策がある）も、同じタイミングで移行を提案する
  1. md の `trial:`（1件）を `trials:` の配列に包み直す。`plan` が要旨3行で ✅/⬜ が無ければ、`⬜ 1. 実現方法を決める` の1項目に置き換える
  2. TRIAL と RECORD の両方にある施策は、**1件ずつ「完了にしますか」と聞く**。はい → RECORD を残して TRIAL から外す（旧 RECORD がある＝本番投入済みなので、plan の ⬜ は無視してよい。予想が無ければ「未記録」）／いいえ → RECORD の項目を消して TRIAL に戻す（旧 RECORD にあった予想・判定日は LOG に退避して捨てない）。**勝手にどちらかに寄せない**（原理3）
  3. 画面の TRIAL を `trials` の件数ぶん `.trial` ブロックに（`references/HTML生成.md`）
  4. md に `summary:` が無ければ、一覧の `.loop-sub` の文を写して足す（1.2.8 から一覧はこれを写す）
- **1.3.0 で分割済みだが `loops/project.css` が無い**（1.3.0 の分割は独自 CSS を落としていた）→ `python3 ~/.claude/skills/rising-loop/assets/split-index.py loops/ --project-css --dry-run` で件数を見せ、「はい」で `--dry-run` なしを実行（退避ファイル `loops/.tmp/index-before-split.html` から作る）。殻と頁に `<link rel="stylesheet" href="project.css" />` が無ければ `shell-update.py` で殻を入れ替え、頁は `rising.css` の link の次の行に足す
- **殻に主要な数字（`.rev`）が無い**（旧「合算が無ければ消す」で消していた）→「タイトル下に主要な数字を置きますか」と聞き、`references/HTML生成.md`「主要な数字」の決め方で候補を1つ出して確認を取ってから置く
- 今後も互換を壊す変更は `CHANGELOG.md` に **移行が必要** と書き、この節に手順を足す

### 新規に `loops/` を作るとき（速さの手順）

目標が決まり「作りますか」に「はい」が来たら、**「数字を更新し、施策を再評価する」と同じ形で子に出す**。親が頁を書かない・子に文書を読ませない・突き合わせは頁の中だけ。

1. **親が数字取りを1回だけ**（README のコマンド、無ければ材料の CSV/DB を集計するコマンドを親が1回書いて実行）→ `loops/.tmp/numbers-<日付>.txt`。**ゴールの数字を「同じ窓で月ごとに数え直した 12 か月ぶん」も、ここで一緒に取る**（頁の履歴の点になる。子に取らせない）。**子に集計させない**（★ 2026-09-18 に子が DB を何度も集計して1頁に 8 分かかった）。**書き出したファイルを開いて、各コマンドの出力が空でないことを確かめてから子を起動する**（★ 同日、一部が空のまま渡して2頁を差し戻した）
2. **親が共通ファイルを置く**: `rising.css` `rising.js` `chat-pane.sh` を `assets/` からコピー。`assets/loopdata.py` は `loops/update/common.py` に
3. **親が1メッセージで子を同時に起動**（model は `opus`）: 殻＋README で1つ、ループごとに1つ。**渡すのは短い要旨だけ**（下の枠。合計 15 行以内。規約の本文・雛形の中身・HTML生成.md の引用を貼らない。★ 2026-09-18 に指示文だけで1万トークン・2.5分かかった）
   > 対象: `loops/L01.html`（雛形 `~/.claude/skills/rising-loop/assets/loop.html` をコピーして埋める）
   > ゴール: <title>／動かしたい数字: <metric.name>／いま: <値>／目標: <値>／窓: <window>／はじめ: <start>
   > 数字: `loops/.tmp/numbers-<日付>.txt`（これ以外を集計しない）
   > 手順: ① 雛形をコピー ② `LOOP_DATA` を埋める（`id title summary updated metric hist funnel trials:[] records:[]`。`hist.days/line/points` は数字ファイルから。**`points[]` には 12 か月ぶんの月ごとの点を積む**。ファネルが無ければ `funnel` を省く） ③ 散文（ゴール下の一文・レポート・ボトルネックの考察と施策案 3〜5 件）を `LOOP_DATA` の値で書く。レポートは「いまの窓の内訳」＋折りたたみで「12 か月の推移の表」。考察は結論1文から始め、レポートに出した内訳の数列を繰り返さない ④ 散文の数字が `LOOP_DATA` と同じことを確かめる（対象は metric・hist・funnel の値だけ） ⑤ `node --check` でインライン script
   > 規則: 結論1文＋折りたたみ、数列は表へ、太字は段落に1つ。仕組みの説明を画面に書かない。雛形の架空の文章と数字を1つも残さない
   > 禁止: 他の頁・index.html を開かない。集計コマンドを書かない。40 ターンを超えない
   > 報告: `summary`・最新点・施策案の見出し・突き合わせの結果（ゼロであること）
   殻の子には: `SERVICE_NAME` `PROJECT_DIR` `PANES`／主要な数字の候補（親が「主要な数字」の決め方で1つ決め、ユーザーの確認済みのもの）／各ループの `id title 最新点 target summary`（子から返る前なので親が決めた値）／README に書く数字の取り方
4. 子が返ったら親が `summary` と最新点を確認し、殻の一覧の行が各頁の値と同じことを確かめる（子の報告を突き合わせるだけ。頁は開かない）
5. URL を渡して締める（「作り終えたら、開ける URL を渡して、そこから始めてもらう」）

目安: 2ループで **3 分以内**。超えたら子の指示文が長いか、子が集計している。

### 毎回やること

1. `loops/` を探す。無ければ、**先に目標の例を10個出して選ばせる**。番号でも自分の言葉でもよい。選ばれたら GOAL の1問（動かしたい数字は何か・どの窓で測るか）に進み、「作りますか」と聞いてから作る（手順は `references/HTML生成.md`「新規に `loops/` を作るとき」）
   > 例えばこんな目標があります。番号か、自分の言葉で教えてください
   1. 申し込みを増やしたい
   2. 見込み客を増やしたい
   3. 翌日また来る人を増やしたい
   4. 有料に切り替える人を増やしたい
   5. 解約を減らしたい
   6. TOEIC の点を上げたい
   7. YouTube の登録者を増やしたい
   8. 株の含み益を増やしたい
   9. 体重を落としたい
   10. 貯金を毎月積みたい
   ★ このフォルダに材料（CSV・表・DB の書き出し・企画書）があれば、目標を聞く前に**ざっと読んで、数字が取れそうなものを2〜3行で言う**。材料が無ければ聞かずに進む
2. **`loops/README.md` を読む。** そのプロジェクトで数字をどう取るかが書いてある。書いていなければ聞いて、書き足す
3. 各ループ頁の `LOOP_DATA`（`summary`・最新点・`trials[]`・`records[]` の `grade`）から、いまの状態を短く報告する。**頁は子に読ませる**。**まず現在地を言う。いきなり作業に入らない**。
   施策案があって TRIAL が空なら、報告の末尾も「まず施策を1つ選んで、『指示する』から『TRIAL に移す』と送ってください」で締める
4. **画面の「ループを追加」から `section: LOOPS / task: ループを追加して: …` が来たら**、その一文を目標の言葉として受け取り、GOAL の1問（動かしたい数字・窓）から始める。ID は既存の次の番号。画面の直し方は `references/HTML生成.md`「ループを追加」（3か所。1つでも抜けると開けない画面になる）
5. **新版が出ていないかを1回だけ見る**（セッションにつき1回。ネットが無ければ黙って続ける）
   ```bash
   cat ~/.claude/skills/rising-loop/VERSION; curl -fsSL --max-time 3 https://raw.githubusercontent.com/toru0325/rising-loop/main/VERSION
   ```
   違っていたら、現在地の報告の末尾に1行だけ添える:
   「rising-loop の新版 X.Y.Z があります。画面の『? 使い方』→『⬆ アップデート』から更新できます」
   - ★ **AI は `install.sh` を自分で実行しない。** `~/.claude/skills` を書き換える操作なので、ユーザーに打ってもらう。
     **例外は、画面の「⬆ アップデート」から来た指示（`task: rising-loop を最新版に更新して、このループを合わせて`）だけ。** 押したのは本人なので、そのときは次の順で自分でやる:
     ① `curl -fsSL https://raw.githubusercontent.com/toru0325/rising-loop/main/install.sh | sh` を実行し、出力（版・変わったこと）をそのまま見せる。「すでに最新版」なら CDN の反映待ちの可能性を1行添える
     ② `/rising-loop` を呼び直して SKILL.md を読み直す
     ③ 「旧形式の `loops/` は、最初に移行を提案する」の「合わせて」を実行する（移行が要る版は、そこでユーザーに聞く）
     ④ 終わったら「画面をリロードしてください。他のループの右ペインでは『このループを合わせて』を1回送ってください」と締める
   - 更新を勧めるのは1回だけ。断られたらそのセッションでは二度と言わない

数字が古ければ、README に書いてあるコマンドを実行して取り直す。
**数字の更新は、GOAL・REPORT・history と各施策の RECORD の見直しをセットで行う**（手順は「数字を更新し、施策を再評価する」）。

---

## ループの型

**GOAL / REPORT / BOTTLENECK / TRIAL / RECORD の5つ。**
出来事の LOG は `loops/logs/<ID>.md` に分離する。

### GOAL — 動かしたい数字

原理2がそのまま形になる場所。

- **いつ取った数字かを必ず持つ**（`metric.measured`）
- **取り直したら、上書きせず `hist.points[]` に1点積む。** 「いま」の値はその最新点なので、`metric` に `now` を持たない
- **窓（分母にする期間）を決めておく。累計で取らない。** 累計だと古い日が分母に残り続け、**何をしても数字が動かなくなる**

```js
points: [
  { at: "2026-08-30", label: "8月30日", value: 40, end: "8/30", note: "診断した日（実質2日・559回）", recs: [] }
]
```

**窓の長さは、ループごとに決める。一律の既定を当てない。目標の単位と揃えるのが基本。**
決めた窓は `metric.window` に書き、画面の散文にも同じ窓を出す。

| 目標 | 窓 |
|---|---|
| 週◯件 | 7日 |
| 月◯円 | 30日 |
| ◯%（率） | 母数が薄くならない長さ。3日程度から |

- ⚠️ **目標と窓の単位がずれると、目盛りが意味を失う。**「週10件」の目盛りに3日ぶんの件数を当てても読めない
- **窓に「今日」を入れる。** 最新を知るために取り直すのに、今日を捨てたら取り直す意味がない。ただし **「今日は集計途中」と必ず書く**。とくに**外部の集計は反映が遅れて低く出る**ので、自前のデータと混ぜるときは、どの段が途中なのかを明記する
- ⚠️ **窓より歴が浅いうちは、何もしなくても日が増えるだけで数字が伸びる。** そのあいだは「まだ◯日ぶん」と書き添える
- **窓の規則を変えたら、過去の点も同じ規則で取り直す。** 違う窓の値を1本の線に並べると、**変化に見えるものが窓の違いでしかない**ことになる

**同じ日に2回目以降の計測をしたら、聞かずにその日の点を上書きする。** 点が増えるほど線が読めなくなるうえ、同じ日の点が並んでも判断は変わらない。最新を知るために取り直しているので、確認で止めない。
**日をまたいだ計測は聞かずに足してよい。** 聞くのは「同じ日に2回目」のときだけ。

数字型（`metric` あり）と、完成型（`metric` なし）の両方ある。**完成型を無理に数字にしない。**「LPを刷新する」は数字ではないが、立派な目標。

### REPORT — いまの数字の内訳

**GOAL の数字が、どこから来たかを見せる場所。**

- 入れるのは **いまの数字の内訳だけ**（枠ごと・日ごと・ファネルなど）
- **打ち手の話は入れない**（それは施策案）。**経緯も入れない**（それは LOG）
- **形は目標ごとに違う。列数も表の作りも決め打ちしない。** その数字を読むのに必要な形にする
- **過去ぶんのレポートは保存しない。** 判断に使わないのに md が膨らむ。**常に最新の1窓ぶんだけ**。ただし施策の比較に使った数字・期間・母数は RECORD に保持する
- **日ごとの値を1つ持つ。** 取れないループは折れ線だけでよい

★ **これがあるので、ボトルネックには表を置かない**（原理1）。「いまどうなっているか」と「これからどうするか」を混ぜない。

### BOTTLENECK — 解決していないこと、ぜんぶ

2つが入る。**この2つ以外の枠を作らない。**

- **考察** — この目標が動くと考えている理由。**未検証の仮説**。逆に振れる可能性も書く
- **施策案** — **何をするか**を、いま効きそうな順に、**最大5件**。素の文章で並べる

- **施策案は「何をするか」で書く。** 「〜できていません」「〜が分かりません」は**問題の記述であって案ではない**。**動詞で終える**（〜を見せる／〜を測れるようにする／〜を試す）。根拠にした数字は案のうしろに括弧で短く添える
- **5件まで**（原理6）。6件目が出てきたら、まとめるか捨てる。**5件目より弱いなら、それは書かなくていい**
- **「確認してほしいこと」「見張っているもの」を独立させない。** 別枠に置くと、そこだけ見なくなる。危ないものほど同じ場所に混ぜる
- **ラベルや分類を作らない。** 「要判断」「案あり」のような enum を発明しない。**書いてある文章がそのまま状態**
- **「いまどうなっているか」だけを書く。経緯・訂正・言い訳を書かない。** ここは現在地を見る場所であって、履歴を見る場所ではない。訂正が判断に影響するなら、内容を LOG に、影響する RECORD の根拠を RECORD に書く
- **そのループの目標に関係ないことを書かない。** 判断は「**この行は、この目標の数字を動かす話か**」の1問で足りる。**これは RECORD にも同じく効く**

### TRIAL — いま動かしているもの

**TRIAL は何件でも並べてよい。** 施策案から拾うたびに1件足す（`LOOP_DATA.trials[]`）。効果を追う過去施策は RECORD に複数持てる。
実施完了と効果の判定は別（原理5）。施策どうしの実施期間や観測期間が重なったら、その影響を評価に明記する。

- **プランは実現手順のチェックリスト。** やることを1行ずつ、3〜7項目。済んだものは `✅`、まだのものは `⬜`（`trials[].plan` も画面も同じ記号）。1つ終えるたびに ✅ に変える。「1〜3まで実装済み」のような文字のステータスより、どこまで進んだかが一目で分かる（2026-09-13 のユーザー指示）。**全部 ✅ が実施完了の合図**
- 手順の本文（触るファイル・決定の経緯）は別の進行ファイルに置き、TRIAL からはそのパスを1行で指す。ループの画面は「いま何を動かしているか」を見る場所で、実装の手順書を読む場所ではない
- **TRIAL に進捗バーを出さない。** `.trial-status` は事実だけ文字で書く。全部 ✅ なら「全部済み。直しがなければ『完了にして』」
- **拾い上げたら、施策案の一覧からは消す**（原理1）。`▶` を付けて残さない

**終わりには2段階ある。勝手に1段目から2段目へ進めない**（原理5）。

| | いつ | どうする |
|---|---|---|
| **1. 実施が終わった** | `plan` が全部 ✅ になった時点。AI が自分で判断してよい | **TRIAL にそのまま止まる。** ステータスを「全部済み。直しがなければ『完了にして』」にする。「完了」「済み」とは書かない。直しが出るかもしれないので、まだ外さない。**RECORD には何も作らない** |
| **2. ユーザーが「完了にして」と言った** | 画面の TRIAL の「指示する」→ 札「完了にして（評価に移す）」。ユーザーの一言だけが合図 | **ここで初めて効果の話をする**（下の「評価に移す」）。答えが揃ったら TRIAL から外し、RECORD に1項目作る |

- ★ **施策は TRIAL か RECORD のどちらか一方にしかいない**（原理1）。実施中に RECORD を先に作らない。同じ施策が2か所に見えると「何を待っているのか」が読めなくなる（★ 実際に「重複する状態が意味不明」と言われた 2026-09-17）
- **「実施が終わった」を「完了にしていい」と読み替えない。** 1段目で外すと、直しを入れる場所が無くなる
- **TRIAL に「完了」の表示を付けない。** 完了は RECORD の `実施済`（完了日つき）だけが持つ
- **2段目はユーザーにしか押せない**（原理3）。数字が出ていなくても、言われたら評価に移す
- 途中でやめる施策は「これは消して」で施策案（ボトルネック）に戻すか消す。⬜ が残ったものを RECORD に入れない。どこまで進んだかは LOG に残す

実施の事実（本番に入れた日・変更内容）は LOG に残す。RECORD はまだ作らない。

### RECORD — 施策の評価

**1施策1項目。数字を取るたびに、同じ項目の現在評価を更新する。** 日付ごとの出来事を並べない。
LOG を掘らなくても、その施策がいまどう評価されているか、根拠と一緒に読めるようにする。

- **保持するもの**：実施日・変更の要旨・狙いと事前の予想・実施前の数字と比較条件。**予想は結果を見て書き換えない**（原理4）
- **更新するもの**：実施後の観測（期間・母数・出典を含む）・現在の評価と評価日・判断の限界・次に確認すること
- **現在の評価の文は 120 字以下。** 根拠・限界・次に見ることは本文へ流す。画面でひと目で読める長さにする
- **現在の評価には5段階を付ける**：**A 狙いどおり ／ B おおむね ／ C 半分 ／ D わずか ／ E 届かず ／ -（未評価）**
  - 付ける基準は**その施策が狙ったことをどれだけ達成したか**で、ループの目標が動いたかではない。
    **計測を入れる施策なら、計測が走った時点で狙いは達成（A）。** 売上が動いたかは別の施策の話
  - 切り分けができない・観測がまだ届かないものは `-` にし、理由を添える
  - ⚠️ **点数（n/10）にしない。** 細かい点差に意味を持たせられず、分母の説明も要る
  - ※ この5段階だけが、このスキルで公認された唯一の enum。ほかに分類を発明しない（原理6）
- **評価待ち**は理由を添える（観測期間が短い、母数不足、計測未着など）。「効かなかった」や、観測を終えた「わからなかった」と混同しない
- **単なる前後差を、施策の因果効果と断定しない**（原理4）。期間・単位・計測方法を揃え、同時変更・流入の変化・曜日・集計途中など、今回の比較に影響する条件を短く添える
- AI の暫定評価は根拠に沿って更新する。**ユーザーが確定した判定は、判定日とともに保持する**（原理3）。後の証拠と食い違えば現在評価にその差を書き、確定判定そのものは勝手に変更しない
- **「効果測定はしない」は評価ではない。段階の代わりに出さない。** 実施済みで狙いが果たされたなら A〜E を付け、本文に「売上への効果は測っていない」と書く。**実施完了を、売上などの改善成功と扱わない**（原理5）

### LOG — 出来事（別ファイル）

**`loops/logs/<ID>.md` に日付順で残す。** 本番変更・外部の出来事・計測の問題・製品について分かった事実など、後で評価を見直す材料だけ。
チャットの経過、細かいファイル編集、案を却下したといった舞台裏を足さない（原理6）。

---

## 中核のフロー

### ボトルネックから拾う

画面の施策案の「指示する」→「TRIAL に移す」（本文は「これを TRIAL に移して手順を出して（実装はしない）」）で始まる。番号だけ来たら、どの案かを確認する。

★ **ここで効果の数字の話をしない。** 施策案はまだアバウトで、どう実現するかも決まっていない。
「見る数字は？」「予想は？」と聞かれても答えようがない（★ 実際に「意味不明」と言われた 2026-09-17）。
効果を確かめるかどうかは、**実施が終わって「完了にして」と言われたときに聞く**（「評価に移す」）。

- **「入れる」「試す」「やる」で済ませない。** 何を作って、どこに出すのかを**その場で言葉にする**。自分の中では自明でも、相手はコードを見ていない

### 「やる」と言われたら、まずステータスを変える。着手しない

**厳守**（原理3）。手順を飛ばさない。

1. **画面のステータスを変える。** その案を **施策案の一覧から消して** TRIAL へ移す（`LOOP_DATA.trials[]` に1件足し、TRIAL の markup にも出す。頁を直すのは子）
2. **その場で実現手順をチェックリスト（`plan`）にして TRIAL に出す。** 3〜7項目、全部 `⬜`。
   どう実現するかが決まっていなければ、**1項目目を「実現方法を決める（案を複数出して選ぶ）」にする**。
   これが TRIAL の中身であり、**✅ が全部埋まったら実施完了**の合図になる
3. 1項目目が「実現方法を決める」なら、アイデアを複数出してユーザーが1つ選ぶ。選ばれたら「これでいいですか」と見せて、承認を得てから書く。決まったら残りの項目を具体に書き直す
4. 項目を1つ終えるたびに `⬜` → `✅`。`trials[].plan` と画面のチェックリストを同じターンで直す

**施策案は常に漠然としている。** 「バナーにボタンを置く」だけでは、**どこに・どんな形で・何と書くか**が決まっていない。
**勝手に1つに縮めて実装しない。** 縮めた時点で、それはユーザーの案ではなく自分の案になっている。

### ★ 作業は子エージェントに出す。窓口は自分でコードを触らない

**この窓口のセッションがやるのは「数字を読む・報告する・聞く」だけ。**
★ **窓口はループ頁を読まない。値は子が `LOOP_DATA` から報告する。**
次は必ず子エージェント（Agent ツール）に出し、戻ってきた結果を確認してから報告する。

| 子に出すもの | 渡すもの |
|---|---|
| 施策の実装（サービスのコード・計測・テスト） | 選ばれたプラン、触ってよい範囲、終わったら diff の要約を返すこと |
| **数字の更新（1ループぶん）** | `update/LXX.py` があれば親がそれを実行し、子には **差分の表と頁のパスだけ**（`LOOP_DATA` の値は触らせない）。無ければ そのループの頁 `loops/LXX.html` のパス・親が取った数字・規約の要点。**その頁を直接直す。殻の `index.html` と他のループの頁は開かない**。**戻すのは `LOOP_DATA` の要約**（最新点・`summary`・各 record の `grade`）（手順は「数字を更新し、施策を再評価する」） |
| **殻・ループ頁の新規生成、構造の変更** | `references/HTML生成.md` を読むこと、**最後に散文の数字を1つずつ `LOOP_DATA` に当てて突き合わせ、結果を返すこと** |
| 複数ファイルにまたがる調査 | 知りたいこと。結論だけ返す |

- 理由：窓口は何か月も resume し続ける。**画面（殻とループ頁を合わせると数千行）を窓口が読むたびに会話が肥大し、いずれ窓口として使えなくなる**。正は頁の中にあるので、窓口は子が返す `LOOP_DATA` の要約だけを受け取る
- 子が返した突き合わせ結果を確認してから「更新した」と報告する。**子に出したことを完了扱いにしない**（原理5）
- 子を立てるときは model を明示する（実装・html 生成は `opus`。定型の置換だけなら `sonnet`）
- ★ **複数の子は1つのメッセージでまとめて起動する。** 1つずつ起動すると、そのたびに親が考える時間（20秒）が積もる（★ 6ループで2分。2026-09-15 の実測）
- ★ **子にスキル本文を読ませない。** 必要な規約は親が要点にして渡す。6人が同じ SKILL.md と README を読むと、1人あたり1分が無駄になる。構造を変える作業だけ `references/HTML生成.md` を読ませる

### 評価に移す（ユーザーの「完了にして」で始まる）

TRIAL の `plan` が全部 ✅ でも、AI からは進めない。ユーザーが TRIAL の「指示する」から **「完了にして（評価に移す）」** を送ったときだけ始める。

**ここで初めて効果の話をする。1問ずつ聞く**（原理3）。

1. > **効果を数字で確かめますか？**
   > - **確かめる** → 次へ
   > - **確かめない** → RECORD に移して終わり。評価は A〜E を「実施が狙いどおりに済んだか」で付け、本文に「売上などへの効果は測っていない」と1行書く。判定日は書かない（実施日のみ）
2. 確かめるなら、3つを**私が仮置きで出して、直してもらう**
   ```
   見る数字      （ボトルネックで指摘した数字）
   いつ判定      （既定は2週間後）
   どうなると思う  （★ここだけは必ず確認を取る）
   ```

**予想を聞くのはここだけ**（原理4 は「判定の前に予想がある」こと）。
★ **それより前に AI から「見る数字は」「予想は」「測っておきますか」と持ちかけない。** 本番に入れる直前でも、全部 ✅ になった瞬間でも、聞かない。
ユーザーが「完了にして」と言うまで、**この施策のための測定も数字取りもしない**（★ 2026-09-17 のユーザー指示。AI のタイミングで聞くのは優先順位の取り違え）。
**ここで甘い予想を勝手に置くと、以後の判定が全部甘くなる。**

- 実施前の数字は GOAL の `history` と日ごとの表から、実施日より前の点を拾って書く。無ければ「未記録」
- 「完了にして」までに「更新」が走って実施後の数字が画面に出ていることはある。それでも予想はここで書く。RECORD の本文に「予想は実施後の数字（◯/◯ 更新）を見た後に記入」と1行添え、判定の限界として持つ

答えが揃ったら:
- その施策を `LOOP_DATA.trials[]` から外し、`records[]` に1件足す（`done`・`summary`・`aim`・`before`・`forecast`＋`forecast_at`・`judge`・`grade`＋`graded`）。TRIAL の markup も RECORD へ移す
- LOG に「評価に移した」事実を残す
- 他に TRIAL が無ければ空の表示にする

- **「経過を見ますか？」と聞かない。** 何を指しているのか伝わらない。**この仕組みでの結論（効果を数字で確かめるのか、確かめないのか）をそのまま聞く**
- 他の施策を観測中なら、今回の実施日と影響し得る数字を、その施策の RECORD にも反映する

### 数字を更新し、施策を再評価する

★ **1ループずつ完結させる。** 横断して「まず全部のGOAL、次に全部のREPORT」とやらない。
★ **親は数字を取って配るだけ。ループの中身は子が直す。** 親は index.html を開かない（「作業は子エージェントに出す」）。

**まず `loops/update/` を見る。** `ls loops/update/` で `LXX.py` があるループは **手順 A**（スクリプトが値を書く）、無いループは **手順 B**（子が書き、終わったらスクリプトを書き残す）。1回の更新で A と B が混ざってよい。

**手順 A: `loops/update/LXX.py` があるループ（2回目以降）**

1. **親が `python3 loops/update/LXX.py` を対象ループぶん実行する**（cwd はプロジェクトの根。数秒〜数十秒）。スクリプトが README のコマンドを叩いて `LOOP_DATA` の値（`days[]`・`line[]`・`points[]`・`metric`・`updated`）を書き換え、**差分の表**を出す。元の頁は `loops/.tmp/LXX-before-update.html` に退避される。★ **親はループ頁を開かない。差分の表だけ読む**
2. **親が子を1つだけ起動する**（model は `opus`。ループが複数なら1ループ1子を1メッセージで）。子に渡すのは **差分の表と頁のパスだけ**。子がやるのは:
   - `LOOP_DATA.summary` を1文（数字入り・60字以内）
   - ゴール下の一文と、窓の数字を写している散文を `LOOP_DATA` の値で直す
   - 判定日が近い・過ぎた施策の評価文（1〜2件）。`records[].grade`/`graded` と markup を揃える
   - `points[最新].note` に窓と件数の1行
   - 子の要旨（そのまま貼る）
     > 差分の表・頁のパス・`summary` と散文と評価文だけ。**`LOOP_DATA` の値は触らない**（スクリプトが書いた値が正）。最後に散文の数字 ⇄ `LOOP_DATA` を突き合わせ、「散文にだけ古い数字」がゼロであることを報告する
3. **スクリプトが失敗したとき**（出力の形が変わった・コマンドが無い・差分の表が明らかにおかしい）: **その回は初回扱い**（手順 B で子が更新する）。**スクリプトも直す**（子の仕事に足す）
4. あとは手順 B の 3〜6 と同じ（突き合わせの確認・一覧・LOG・`.tmp`）

★ **`LOOP_DATA` の計算（窓・分母・単価）を変えたら `update/LXX.py` も直す。** 片方だけ直すと次回ずれる。右ペインで頼まれた AI は頁と `update/LXX.py` の両方を直す。

**手順 B: `loops/update/LXX.py` が無いループ（初回・ループを追加した直後・スクリプトが失敗した回）**

1. **親が数字取りを1回だけ実行する。** `loops/README.md` のコマンドを全部叩き、生の出力を `loops/.tmp/numbers-<日付>.txt` に保存する。6人の子が同じコマンドを叩かない
2. **親が1メッセージでループの数だけ子を起動する**（model は `opus`）。1つの子＝1ループ。渡すのは3つだけ:
   - そのループの頁 `loops/LXX.html` のパス（子が読み、**そのファイルを直接直す**。殻の `index.html` と他のループの頁は開かない）
   - 手順1の数字ファイル
   - 規約の要点（次の9行をそのまま貼る）
     > ① README の取り方で取った数字を使う。同日の計測点は上書き ② **先に `LOOP_DATA` を直す**（`hist.points[]` に点を積む・`days[]`・`line[]`・`funnel`・`summary`・`metric.measured`/`window`・`updated`）③ 次に頁を最初から最後まで読み直し、前の窓の数字を見出しに関係なく全部直す（ゴール下の一文・考察・施策案・観測・数字を埋めた文章すべて）④ 前回の評価以降の LOG（`logs/LXX.md`）を読む ⑤ 各施策の現在評価（A〜E・120字以内・評価日）を見直し、`records[].grade`/`graded` と評価文の markup を揃える。窓が実施前後をまたぐ値を「実施後」と扱わない。判断できなければ理由と次に必要な観測を書き、証拠が無ければ数字と評価日を保つ ⑥ 結論1文＋折りたたみ、数列は表へ、太字は段落に1つ ⑦ `LOOP_DATA.summary` に一覧用の一文（数字入り・60字以内）を書く ⑧ 最後に**頁の中で突き合わせる**（散文の数字 ⇄ `LOOP_DATA`）。対象は `metric`・`hist`（`days`/`line`/`points`）・`funnel` の値だけで、**派生値（割合・倍率・差分）と RECORD の観測表は markup が正なので対象外**。「散文にだけ古い数字」がゼロであることを報告する ⑨ **今回の計算を `loops/update/LXX.py` に書いて残す。** `from common import load, save, merge_days, add_point`（`loops/update/common.py` が無ければ `cp ~/.claude/skills/rising-loop/assets/loopdata.py loops/update/common.py`）。数字取りは README のコマンドを `subprocess` で叩き、`LOOP_DATA` の値の計算をそのまま Python にする。`python3 loops/update/LXX.py --dry-run` の差分の表が今回の値と一致する（変更なし）ことを確かめてから残す。使い方は `common.py` の先頭にある
3. **子の突き合わせ結果を確認する。** 「直したつもり」で終えない（原理5）。ゼロでなければその子に差し戻す。**`update/LXX.py` が残っていること**（`--dry-run` で変更なし）も確認する
4. **最後に一覧を直す。** 子を1つ（`sonnet` でよい）。**殻 `loops/index.html` の `<!-- LOOPS:BEGIN -->` 〜 `<!-- LOOPS:END -->` の中だけを直す**（行の数字・ゲージ・`.loop-sub`＝`summary`・タイトル下の主要な数字 `.rev`。主要な数字は README の取り方で取り直す）。渡すのは各頁の `LOOP_DATA` の要約（`summary`・最新点・`hist.target`・施策の件数）と、そのブロックの中身だけ
5. 評価が大きく変わったループだけ LOG に変更前後と根拠を残す（子の報告から）
6. `loops/.tmp/` は消してよい（正ではない）

★ **差し替える場所をチェックリストにしない。** 置き場所は増えるし、見出しの構成も変わる。守るのは「**このループの頁に、前の窓の数字が1つも残っていない**」という結果だけ。判断は「**この数字は、いま取り直した窓のものか**」の1問で足りる。

★ **窓の数字は本文にも書き写されている。** ゴールの下の一文、ボトルネックの考察と施策案、施策の観測、一覧の行——**数字を文章に埋めた場所は、そのまま更新対象になる**。

★ なぜこの形か（2026-09-15 の実測、6ループで31分）: 画面の更新を1つの子が6画面直列でやって15分、子を1つずつ起動して2分、6人が同じスキル本文と README を読んで6分、親の文脈が60万トークンで思考が遅い。上の手順なら 6〜7分。
★ なぜスクリプトか（2026-09-22）: 手順 B を4回測って 19〜31分。規約でもモデルでも変わらなかった。数字取りの出力を読んで `LOOP_DATA` に書く計算は同じループなら毎回同じなので、AI が一度 Python に残して次回から実行するだけにする（汎用の対応表は作らない。6ループの計算が全部違い、記法を作ればそれが小さな言語になる）。見込みはスクリプト30秒＋子2〜3分＋一覧1分 ≒ 4〜5分。

### 判定を確定する

更新・相談時に判定期日を過ぎていたら声をかける。**このスキル単体には定期起動・通知機能はない。**
**予想と実際、同期間の他の変更、比較の限界を並べて示す。** AI の現在評価と、ユーザーが確定する判定を区別する（原理3）。

効いた ／ わからなかった ／ 効かなかった ／ 逆効果、の4択から推しと理由を伝え、**決めるのは本人**。
「わからなかった」は計測の穴・他の変更・母数不足などで判断できなかった結果であり、「効かなかった」ではない。
最後に来月の自分に残すひとことをもらい、同じ RECORD に確定判定・日付・理由を保持する。LOG にも確定した事実を残す。
判定後も RECORD は消さず、新しい証拠があれば現在評価を見直す。

---

## 置き場所の仕分け

原理1の適用表。迷ったらこれに当てる。

| 種類 | 置き場所 |
|---|---|
| いまの数字の内訳（最新の1窓ぶん） | **REPORT** |
| 時間とともにどう動いたか | GOAL の数字と折れ線 |
| 施策がいまどう評価されるか・その根拠 | RECORD |
| いつ何があったか | LOG（`loops/logs/<ID>.md`） |
| これからどうするか | BOTTLENECK の施策案 |
| 生データ（CSV など） | ループには置かない。`LOOP_DATA` に写した値が正 |
| 仕組み・規約（窓の決め方、画面の作り） | **`SKILL.md` / `references/`。ループに書かない**（原理7） |
| プロジェクト固有の数字の取り方 | `loops/README.md` |

判断は「**これは、いまの詰まりを示しているか。それとも経過か**」の1問で足りる。

**フィードバックが来たら、まず切り分ける。「このループの中身の話」か「仕組みの話」か**（原理7）。
ループに持たせるのは**規則を当てはめた結果の値**だけ（`window: 8/29〜8/31`、`value: 37`）。**規則そのものは持たせない。**

---

## データの持ち方

**ループの正は `loops/LXX.html`**（原理1）。頁は2つでできている。

- **`LOOP_DATA`（値の正）** — 数字・タイトル・`summary`・`trials[]`・`records[]`。定義は `references/HTML生成.md`「`LOOP_DATA` の定義」
- **markup（画面）** — 散文・表・図。ここに出る `metric`・`hist`・`funnel` の数字は `LOOP_DATA` の**写し**

md で残るのは2つだけ。**`loops/logs/LXX.md`（出来事の正）と `loops/README.md`（数字の取り方）。どちらも画面に出ない。**

- **値を直したら、同じターンで散文も直す。** 突き合わせは**頁の中**（散文の数字 ⇄ `LOOP_DATA`）で、ファイル間ではない
- **散文の数字だけを直さない。** 数字を変えるときは必ず `LOOP_DATA` から
- **派生値（割合・倍率・差分）と RECORD の観測表の数字は markup が正。** `LOOP_DATA` に持たせない（持たせると `records[]` が本文と同じ大きさになる）
- **殻の一覧の行は各頁の写し**（頁の中の写しと並んで、残る二重。気づける形に畳んである）。1つのループを直したら、同じターンで一覧も直す
- 頁の中身は部分的に差し替える。全文を書き直さない
- ★ **差し替えたら、タグの開閉を数えて確かめてから返す**（`references/HTML生成.md`「更新するとき」の検算）。
  `</div>` が1つ余るだけで後ろのセクションが枠の外にはみ出し、背景色が変わる。見た目では気づけない（2026-09-10 に実際にやった）
- 頁の先頭のコメントには「**このファイルがこのループの正**」と書く（「直接編集しないでください」とは書かない）
- LOG 全文をループ頁に複製しない

---

## 書き方

### ★ 一目で終わりが見えない塊は読まれない

人は段落を読む前に「見る」。終わりが見えない塊は、中身を読む前に飛ばされる。**正しさは関係なく、量の時点で情報が死ぬ**（原理6）。md も画面も同じ。

- **1ブロック＝1つの事実、3行以内。先頭に結論1文。** 1段落に事実を3つ入れない
- **数字は文章に埋めない。** 表・棒・ファネル・単独の大きな数字で見せる。「9/7 9回・7回／9/8 17回・14回…」のような数列を文に書かない。誰も読まないうえ、その段落ごと飛ばされる
- **説明・計測の注意・生データ・通算は折りたたむ。** 判断に要らないものは見える場所に置かない。開けば読める、でいい
- **太字は1ブロックに1つ。** 10箇所あると、どれも強調ではなくなる
- 削除ではなく移動。**数字と事実は落とさない**。畳んだ中に全部残す

**平易な日本語で書く**（原理6）。「HPを見た」「予約した」「最後まで遊べた」のレベル。
`CVR` `リテンション` `ファネル離脱` のような語を持ち込まない。**来月これを読むのは本人で、そのとき専門語は思い出す手間になるだけ。**

数字は必ず**何の数字か**とセットで書く（原理2）。`29%` ではなく `完走率 29%（63回始まって18回終わった）`。
**数字が無いのに断定しない。**「たぶん」「未確認」はそう書く。

**ユーザーの指示なく `git commit` しない。**

---

## 参照

| ファイル | いつ読むか |
|---|---|
| `references/HTML生成.md` | 画面（殻・ループ頁・共通ファイル）を作る・更新するとき（画面の作りは全部こちら）。**右ペインの設置も必須手順としてここにある** |
| `references/なぜこの規約か.md` | 規約の理由を知りたいとき・規約を足したいとき |
| `assets/index.html` | 殻の雛形。`loops/index.html` を新規に作るとき |
| `assets/loop.html` | ループ頁の雛形。`LXX.html` を新規に作るとき |
| `assets/rising.css` / `assets/rising.js` | 共通の CSS と JS。**そのまま `loops/` にコピーする**（値を埋めない） |
| `assets/split-index.py` | 1.2.x の単一 `index.html` を、殻＋ループ頁＋共通ファイルに分ける（1.3.0 への移行） |
| `assets/merge-md.py` | `LXX.md` の中身をループ頁の `LOOP_DATA` に取り込む（1.5.0 への移行。1ループずつ） |
| `assets/shell-update.py` | 「このループを合わせて」で、殻を雛形と入れ替える（CONST と LOOPS だけ引き継ぐ）。共通ファイル4つ（`update/common.py` を含む）も上書き |
| `assets/loopdata.py` | `LOOP_DATA` の値だけを読み書きする部品（`load` `save` `merge_days` `add_point`）。プロジェクトの `loops/update/common.py` に写し、ループごとの `update/LXX.py` が使う |
| `assets/chat-pane.sh` | 右ペインのランチャー。プロジェクトの `loops/chat-pane.sh` に写して使う |
| `CHANGELOG.md` / `VERSION` | 版を上げるとき。互換を壊す変更は **移行が必要** と書き、「旧形式の `loops/`」節にも手順を足す |
