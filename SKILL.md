---
name: rising-loop
description: "リリース済みサービスの運営・改善ループを回す進行管理スキル。実データ（アクセス解析・DB・売上等）を見て診断し、ボトルネックを洗い出し、そこから1つ選んで打ち手を実行し、施策ごとの現在評価を計測のたびに更新する——これを複数の目標について並行して回す。現在地と施策評価は各プロジェクトの `loops/*.md`、出来事は `loops/logs/*.md` に持ち、閲覧用の `loops/index.html` を生成する。ユーザーが「運営どうなってる」「数字を見て」「ユーザー動向は」「改善ループ」「ボトルネック」「施策を打ちたい」「効果を測りたい」「KPI」「rising-loop」「ライジング・ループ」「loop-manager」「ループを回して」「リリース後どう」等と言ったときに必ず使う。明示的に頼まれなくても、**リリース済みのサービスについて数字の話が出たら積極的に発動してよい**。除外: これから作る機能の進行管理は、そのための進行管理スキルがあればそちらへ渡す。"
user-invokable: true
---

# rising-loop — 運営・改善ループ

数字を見て、詰まっているところを見つけ、1つだけ手を打ち、経過を見て、施策ごとに効いたかどうかの現在評価を更新する。これを終わりなく回す。

## 7つの原理

**以下の規約は、すべてこの7つから導かれている。** 迷ったとき、規約に書いていない場面に出たときは、ここに戻って考える。

1. **正は一つ。置き場所は役割で決まる** — 同じことを2箇所に書かない。二重に持つと必ずズレて、**ズレたことに気づかない**
2. **数字は「いつ・どの窓・何の」とセットでしか意味を持たない** — 裸の数字は、古いのか新しいのか、何と比べたのかが分からないまま判断に使われる
3. **決めるのはユーザー。AI は選択肢を出して止まる** — 「やる」は着手の合図であって、実装の承認ではない
4. **予想を先に書く。後知恵で判定しない** — 数字を見てから「効いた」と言うのは、いくらでもできてしまう
5. **終わっていないものを、終わったことにしない** — 実施が終わったことと、効果が確かめられたことは別
6. **読む人の負荷に上限を置く** — 来月これを読むのは本人。並ぶほど読まなくなる
7. **規則はスキルに、値はループに** — 規則をループに書くと、ループを増やすたびに全部へ同じことを書く羽目になる

なぜその規約になったのか（実際に起きた失敗）は `references/なぜこの規約か.md`。

## 何を扱うスキルか

**すでに動いているもの**を扱う。これから作るものは扱わない。

| | 作る側の進行管理 | rising-loop |
|---|---|---|
| 対象 | **これから作るもの** | **すでに動いているもの** |
| 終わり | 完成したら閉じる | **終わらない** |
| 反応 | すぐ返る（テストが通る／通らない） | **数日〜数週遅れて返る**（ユーザーの行動） |

作る話が出てきたら、そのための進行管理の仕組みがあればそちらへ渡す（原理1）。**両方に同じことを書かない。**

---

## 最初にやること

### ★ 新しく作るときは、先に道具を確認する

**画面の右ペイン（AIチャット）は必須。** 画面のボタンはすべて「文言をコピーするだけ」の作りなので、
**貼る先がその場に無いと往復が成立しない**。作りはじめる前に、必ずこれを調べる。

```bash
command -v ttyd && ttyd --version              #=== ペインの中身。1.7 以上が要る
command -v claude || ls ~/.local/bin/claude    #=== 相談相手そのもの（PATH に居ないことがある）
command -v codex   #=== codex で使うなら（任意。無くても止めない）
```

- **入っていなければ、そこで止めてユーザーに入れてもらう。** macOS なら `brew install ttyd`。
  **勝手にインストールしない**（環境を変える操作なので、頼まれるまでやらない）
- 入ったのを確認してから続ける
- ★ **「無いから右ペイン抜きで作る」を選ばない。** 中核が欠けた画面ができあがる。
  しかも **CSS だけ残って markup と JS が落ちる**という壊れ方をするので、見た目では気づけない
- どうしても入らない環境で省くときは、**その旨を `loops/README.md` に1行書く。**
  次に触る人が「入れ忘れた」と誤解しないようにする
- ★ **ttyd の起動はユーザーのターミナルで。AI は起動コマンドを見せるだけで、自分で起動しない**（Claude Code の中から起動すると会話が保存されなくなる。理由は `references/HTML生成.md`）

### ★ 作り終えたら、開ける URL を渡して、そこから始めてもらう

**作っただけで終わらない。** 導入してループを作ったら、**クリックできる URL** を渡し、
**以降はその画面が入口になる**ことを伝える。

```
file:///Users/…/プロジェクト/loops/index.html
```

- ⚠️ **裸のファイルパスを貼らない。** そのままではクリックできない。**必ず先頭に `file://` を付ける**
  （パスにスペースがあるときだけ `%20` に替える）
- 「**まずこれを開いてください。次からはこの画面から始めます**」と一言添える
- 使い方も1〜2行だけ添える：**ボタンはすべてコピー動作**なので、押してコピーした文言を
  **右ペインのチャットに貼る**。数字を取り直したいときは `更新`
- 右ペインは、画面の `💡` から**コマンドをコピーして自分のターミナルで実行**してもらう
  （`sh …/loops/chat-pane.sh claude` または `… codex`）。AI は実行しない
- ★ **md のパスを渡さない。** 見る場所は画面であって md ではない（原理1）
- 右ペインが真っ黒なときの直し方は画面の中に書いてある（`💡 15秒たってもチャットが出ない？`）ので、
  **チャットの返答で手順を説明し直さない**

### ★ 旧形式の `loops/` は、最初に移行を提案する

2026-09-11 より前に作った `loops/` は、右ペインの仕組みが古く**そのままでは繋がらない**。
`loops/` を見つけたら、作業に入る前にこれを確かめる。

```bash
grep -l 'var SESS' loops/index.html; ls loops/chat-pane.sh
```

- `var SESS` があり `loops/chat-pane.sh` が無ければ**旧形式**。「右ペインの仕組みが新しくなっています。先に移行しますか」と1問だけ聞く
- 移行の中身は3つ。**子エージェントに出す**（手順の詳細は `references/HTML生成.md` の右ペイン節）
  1. スキルの `assets/chat-pane.sh` を `loops/chat-pane.sh` に置く
  2. `index.html` の右ペイン部分（`💡` の案内ブロックと `<script>` の `SESS`〜iframe 生成）を `assets/template.html` の同じ部分に差し替え、`PROJECT_DIR` と `PANES` を埋める
  3. 旧 `SESS` に入っていたセッションIDを `loops/.chat-sessions` に `<画面ID> claude <ID>` で書き移す（会話を捨てない）
- 終わったら、ユーザーのターミナルで `sh …/loops/chat-pane.sh claude` を立て直してもらう。AI は起動しない
- 今後も互換を壊す変更は `CHANGELOG.md` に **移行が必要** と書き、この節に手順を足す

### 毎回やること

1. `loops/` を探す。無ければ「作りますか」と聞いてから作る（`references/ファイル形式.md` の雛形を使う）
2. **`loops/README.md` を読む。** そのプロジェクトで数字をどう取るかが書いてある。書いていなければ聞いて、書き足す
3. `loops/*.md` を読んで、いまの状態を短く報告する。**まず現在地を言う。いきなり作業に入らない**
4. **新版が出ていないかを1回だけ見る**（セッションにつき1回。ネットが無ければ黙って続ける）
   ```bash
   cat ~/.claude/skills/rising-loop/VERSION; curl -fsSL --max-time 3 https://raw.githubusercontent.com/toru0325/rising-loop/main/VERSION
   ```
   違っていたら、現在地の報告の末尾に1行だけ添える:
   「rising-loop の新版 X.Y.Z があります。ターミナルで `curl -fsSL https://raw.githubusercontent.com/toru0325/rising-loop/main/install.sh | sh`」
   - ★ **AI は `install.sh` を自分で実行しない。** `~/.claude/skills` を書き換える操作なので、ユーザーに打ってもらう
   - 更新を勧めるのは1回だけ。断られたらそのセッションでは二度と言わない

数字が古ければ、README に書いてあるコマンドを実行して取り直す。
**数字の更新は、GOAL・REPORT・history と各施策の RECORD の見直しをセットで行う**（手順は「数字を更新し、施策を再評価する」）。

---

## ループの型

**GOAL / REPORT / BOTTLENECK / TRIAL / RECORD の5つ。**
出来事の LOG は `loops/logs/<ID>.md` に分離する。

### GOAL — 動かしたい数字

原理2がそのまま形になる場所。

- **いつ取った数字かを必ず持つ**（`metric.measured`）
- **取り直したら、上書きせず `history` に1行積む。** `now` を書き換えるだけだと前の値が消える
- **窓（分母にする期間）を決めておく。累計で取らない。** 累計だと古い日が分母に残り続け、**何をしても数字が動かなくなる**

```yaml
history:
  - at: 2026-08-30
    value: 40
    note: 診断した日（実質2日・559回）
```

**窓の長さは、ループごとに決める。一律の既定を当てない。目標の単位と揃えるのが基本。**
決めた窓は `metric.window` に書いて、md と画面の両方に出す。

| 目標 | 窓 |
|---|---|
| 週◯件 | 7日 |
| 月◯円 | 30日 |
| ◯%（率） | 母数が薄くならない長さ。3日程度から |

- ⚠️ **目標と窓の単位がずれると、目盛りが意味を失う。**「週10件」の目盛りに3日ぶんの件数を当てても読めない
- **窓に「今日」を入れる。** 最新を知るために取り直すのに、今日を捨てたら取り直す意味がない。ただし **「今日は集計途中」と必ず書く**。とくに**外部の集計は反映が遅れて低く出る**ので、自前のデータと混ぜるときは、どの段が途中なのかを明記する
- ⚠️ **窓より歴が浅いうちは、何もしなくても日が増えるだけで数字が伸びる。** そのあいだは「まだ◯日ぶん」と書き添える
- **窓の規則を変えたら、過去の点も同じ規則で取り直す。** 違う窓の値を1本の線に並べると、**変化に見えるものが窓の違いでしかない**ことになる

**同じ日に2回目以降の計測をしたら、聞かずにその日の点を上書きする。** 点が増えるほど線が読めなくなるうえ、同じ日の点が並んでも判断は変わらない。最新を知るために取り直しているので、確認で止めない。
**日をまたいだ計測は聞かずに足してよい。** 聞くのは「同じ日に2回目」のときだけ。

数字型（`metric` あり）と、完成型（`metric` なし）の両方ある。**完成型を無理に数字にしない。**「LPを刷新する」は数字ではないが、立派な目標。

### REPORT — いまの数字の内訳

**GOAL の数字が、どこから来たかを見せる場所。**

- 入れるのは **いまの数字の内訳だけ**（枠ごと・日ごと・ファネルなど）
- **打ち手の話は入れない**（それは施策案）。**経緯も入れない**（それは LOG）
- **形は目標ごとに違う。列数も表の作りも決め打ちしない。** その数字を読むのに必要な形にする
- **過去ぶんのレポートは保存しない。** 判断に使わないのに md が膨らむ。**常に最新の1窓ぶんだけ**。ただし施策の比較に使った数字・期間・母数は RECORD に保持する
- **日ごとの値を1つ持つ。** 取れないループは折れ線だけでよい

★ **これがあるので、ボトルネックには表を置かない**（原理1）。「いまどうなっているか」と「これからどうするか」を混ぜない。

### BOTTLENECK — 解決していないこと、ぜんぶ

2つが入る。**この2つ以外の枠を作らない。**

- **考察** — この目標が動くと考えている理由。**未検証の仮説**。逆に振れる可能性も書く
- **施策案** — **何をするか**を、いま効きそうな順に、**最大5件**。素の文章で並べる

- **施策案は「何をするか」で書く。** 「〜できていません」「〜が分かりません」は**問題の記述であって案ではない**。**動詞で終える**（〜を見せる／〜を測れるようにする／〜を試す）。根拠にした数字は案のうしろに括弧で短く添える
- **5件まで**（原理6）。6件目が出てきたら、まとめるか捨てる。**5件目より弱いなら、それは書かなくていい**
- **「確認してほしいこと」「見張っているもの」を独立させない。** 別枠に置くと、そこだけ見なくなる。危ないものほど同じ場所に混ぜる
- **ラベルや分類を作らない。** 「要判断」「案あり」のような enum を発明しない。**書いてある文章がそのまま状態**
- **「いまどうなっているか」だけを書く。経緯・訂正・言い訳を書かない。** ここは現在地を見る場所であって、履歴を見る場所ではない。訂正が判断に影響するなら、内容を LOG に、影響する RECORD の根拠を RECORD に書く
- **そのループの目標に関係ないことを書かない。** 判断は「**この行は、この目標の数字を動かす話か**」の1問で足りる。**これは RECORD にも同じく効く**

### TRIAL — いま動かしているもの

**同時に実行する TRIAL は1ループにつき1つ。** 効果を追う過去施策は RECORD に複数持てる。
実施完了と効果の判定は別（原理5）。次の施策が過去施策の観測期間に重なったら、その影響を評価に明記する。

- **プランは要旨だけ（3行まで）。** 手順・触るファイル・決定の経緯は別の進行ファイルに置き、TRIAL からはそのパスを1行で指す。ループの画面は「いま何を動かしているか」を見る場所で、実装の手順書を読む場所ではない
- **TODO のプランは、やることを1行ずつチェックボックス付きで並べる。** 済んだものは `✅`、まだのものは `⬜`（md も画面も同じ記号）。1つ終えるたびに ✅ に変える。「1〜3まで実装済み」のような文字のステータスより、どこまで進んだかが一目で分かる（2026-09-13 のユーザー指示）
- **TRIAL に進捗バーを出さない。** 判定日がある施策は `.trial-status` に「本番に入れた（9/2）・判定 9/16」のように文字で書く
- **拾い上げたら、施策案の一覧からは消す**（原理1）。`▶` を付けて残さない

**終わりには2段階ある。勝手に1段目から2段目へ進めない**（原理5）。

| | いつ | どうする |
|---|---|---|
| **1. 実施が終わった** | 作業が済んだ時点。AI が自分で判断してよい | **TRIAL にそのまま残す。ステータスは「本番に入れた（日付）」のような事実だけ。「完了」「済み」とは書かない。** 直しが出るかもしれないので、まだ外さない |
| **2. ユーザーが「完了にして」と言った** | ユーザーの一言だけが合図 | **TRIAL から外して RECORD へ移し、観察・評価に入れる** |

- **「実施が終わった」を「完了にしていい」と読み替えない。** 1段目で外すと、直しを入れる場所が無くなる
- **TRIAL に「完了」の表示を付けない。** 付けると「完了なのに TRIAL にある」と読めて、2段目を押したのか押していないのか分からなくなる。完了は RECORD の `実施済`（完了日つき）だけが持つ
- **2段目はユーザーにしか押せない**（原理3）。数字が出ていなくても、言われたら外して RECORD で観察を続ける
- ユーザーが「入れ替える」を選んで次を拾ったときも TRIAL からは外れる（RECORD に残っていることを確認してから）

**本番へ入れた時点で RECORD に施策を1件作り、LOG に実施の事実を残す。** 次を拾うまで作成を待たない。
効果を確かめる施策は、予想を書いた時点で RECORD の項目ができているので、そこに実施日を足す（項目を増やさない）。

### RECORD — 施策の評価

**1施策1項目。数字を取るたびに、同じ項目の現在評価を更新する。** 日付ごとの出来事を並べない。
LOG を掘らなくても、その施策がいまどう評価されているか、根拠と一緒に読めるようにする。

- **保持するもの**：実施日・変更の要旨・狙いと事前の予想・実施前の数字と比較条件。**予想は結果を見て書き換えない**（原理4）
- **更新するもの**：実施後の観測（期間・母数・出典を含む）・現在の評価と評価日・判断の限界・次に確認すること
- **現在の評価の文は 120 字以下。** 根拠・限界・次に見ることは本文へ流す。画面でひと目で読める長さにする
- **現在の評価には5段階を付ける**：**A 狙いどおり ／ B おおむね ／ C 半分 ／ D わずか ／ E 届かず ／ -（未評価）**
  - 付ける基準は**その施策が狙ったことをどれだけ達成したか**で、ループの目標が動いたかではない。
    **計測を入れる施策なら、計測が走った時点で狙いは達成（A）。** 売上が動いたかは別の施策の話
  - 切り分けができない・観測がまだ届かないものは `-` にし、理由を添える
  - ⚠️ **点数（n/10）にしない。** 細かい点差に意味を持たせられず、分母の説明も要る
  - ※ この5段階だけが、このスキルで公認された唯一の enum。ほかに分類を発明しない（原理6）
- **評価待ち**は理由を添える（観測期間が短い、母数不足、計測未着など）。「効かなかった」や、観測を終えた「わからなかった」と混同しない
- **単なる前後差を、施策の因果効果と断定しない**（原理4）。期間・単位・計測方法を揃え、同時変更・流入の変化・曜日・集計途中など、今回の比較に影響する条件を短く添える
- AI の暫定評価は根拠に沿って更新する。**ユーザーが確定した判定は、判定日とともに保持する**（原理3）。後の証拠と食い違えば現在評価にその差を書き、確定判定そのものは勝手に変更しない
- **「効果測定はしない」は評価ではない。段階の代わりに出さない。** 実施済みで狙いが果たされたなら A〜E を付け、本文に「売上への効果は測っていない」と書く。**実施完了を、売上などの改善成功と扱わない**（原理5）

### LOG — 出来事（別ファイル）

**`loops/logs/<ID>.md` に日付順で残す。** 本番変更・外部の出来事・計測の問題・製品について分かった事実など、後で評価を見直す材料だけ。
チャットの経過、細かいファイル編集、案を却下したといった舞台裏を足さない（原理6）。

---

## 中核のフロー

### ボトルネックから1つ拾う

番号で選んでもらう。選ばれたら、**必ずこの1問を聞く。**

> **効果を数字で確かめますか？**
> - **確かめる** → 本番に入れる前に、RECORD に **見る数字・実施前の数字・判定日（既定は2週間後）・予想** を書く（★予想だけは必ず確認を取る）
> - **確かめない** → RECORD に「効果測定はしない」と書く

これが分岐点。**作業量ではなく「効果を測る必要があるか」で分かれる。**

- **「経過を見ますか？」と聞かない。** 何を指しているのか伝わらない。**この仕組みでの結論（効果を数字で確かめるのか、確かめないのか）をそのまま聞く**
- **「入れる」「試す」「やる」で済ませない。** 何を作って、どこに出すのかを**その場で言葉にする**。自分の中では自明でも、相手はコードを見ていない

### 「やる」と言われたら、まずステータスを変える。着手しない

**厳守**（原理3）。手順を飛ばさない。

1. **画面のステータスを変える。** その案を **施策案の一覧から消して** TRIAL へ移し、`md` と `index.html` を更新する
2. **その案をどう実行するかのアイデアを、複数出す。** これが TRIAL の **プラン** になる
3. ユーザーが1つ選ぶ
4. 選ばれたものを「これでいいですか」と見せて、承認を得てから書く

**施策案は常に漠然としている。** 「バナーにボタンを置く」だけでは、**どこに・どんな形で・何と書くか**が決まっていない。
**勝手に1つに縮めて実装しない。** 縮めた時点で、それはユーザーの案ではなく自分の案になっている。

### ★ 作業は子エージェントに出す。窓口は自分でコードを触らない

**この窓口のセッションがやるのは「数字を読む・報告する・聞く・`loops/*.md` を直す」だけ。**
次は必ず子エージェント（Agent ツール）に出し、戻ってきた結果を確認してから報告する。

| 子に出すもの | 渡すもの |
|---|---|
| 施策の実装（サービスのコード・計測・テスト） | 選ばれたプラン、触ってよい範囲、終わったら diff の要約を返すこと |
| **`index.html` の生成・更新** | 直した md、`references/HTML生成.md` を読むこと、**最後に md の数字を1つずつ html に当てて突き合わせ、結果を返すこと** |
| 複数ファイルにまたがる調査 | 知りたいこと。結論だけ返す |

- 理由：窓口は何か月も resume し続ける。**`index.html` は1,000行を超えるので、窓口が読むたびに会話が肥大し、いずれ窓口として使えなくなる**。md（正）は小さいので自分で直してよい
- 子が返した突き合わせ結果を確認してから「更新した」と報告する。**子に出したことを完了扱いにしない**（原理5）
- 子を立てるときは model を明示する（実装・html 生成は `opus`。定型の置換だけなら `sonnet`）

### 効果を確かめないとき

効果を確かめない施策も TRIAL 枠で実施を管理する。本番に入れたらステータスに事実だけを書き、RECORD と LOG を更新する。
TRIAL から外すのは、ユーザーが「完了にして」と言ったときだけ（TRIAL の2段階）。
他の施策を観測中なら、今回の変更の実施日と影響し得る数字を、その施策の RECORD に反映する。
期間も予想も付けない。RECORD には施策の要旨と「効果測定はしない」を残す。

### 効果を確かめるとき

3つ決める。**私が仮置きで出して、直してもらう。決まったら RECORD に書く**（実施前の数字も一緒に残す）。

```
見る数字      （ボトルネックで指摘した数字）
いつ判定      （既定は2週間後）
どうなると思う  （★ここだけは必ず確認を取る）
```

**予想を先に書かせる**（原理4）。後から数字を見て「効いた」と言うのは、いくらでもできてしまう。書いた予想があって初めて判定になる。**ここで甘い予想を勝手に置くと、以後の判定が全部甘くなる。**

**TRIAL 枠が埋まっているとき**は二択で聞く。ユーザーが「完了にして」と言って枠が空いていれば、そのまま次を入れる。過去施策の評価は RECORD で続ける。

```
[ 待つ ]        いまの試行が終わったら、これを次に始めます
[ 入れ替える ]   いまの実行を止めて切り替えます。実施済みの部分と観測への影響を残します
```

「待つ」を選んでも、**順番待ちの列は作らない。** ボトルネックに残るだけ。列を作ると、それ自体が管理対象になる。

### 本番に入れたら

施策の実施日と変更内容を LOG に残し、RECORD に書く（予想を書いた項目があればそれに足す。無ければ作る）。実施前の数字・予想・比較条件はこの時点で揃っているようにする。
実施が終わったら TRIAL に**そのまま残す**（ステータスは「本番に入れた（日付）」の事実だけ）。RECORD の評価がまだ待ちでもよい。未実施の案は実施済みの RECORD に混ぜない。
**TRIAL から外すのは、ユーザーが「完了にして」または「入れ替える」と言ったとき。AI の判断では外さない。**

### 数字を更新し、施策を再評価する

★ **1ループずつ完結させる。** 横断して「まず全部のGOAL、次に全部のREPORT」とやらない。

1. README の取り方で数字を取得する。同日の計測点は GOAL の規則に従って上書きする
2. **その md を最初から最後まで読み直す。** 前の窓の数字が残っている箇所を、見出しに関係なく全部直す
3. 前回の評価以降の LOG を読む。計測日と評価日は別なので、`metric.measured` だけで既読範囲を決めない
4. **各施策の現在評価を見直す。** 比較期間・母数・出典を確認し、RECORD の同じ項目を更新する。REPORT の窓が実施前後をまたぐなら、その値をそのまま「実施後」と扱わない
5. 判断できなければ理由と次に必要な観測を書く。計測失敗・新しい証拠なしなら実測を作らず、既存の数字と評価日を保つ
6. **同じループの画面（`index.html`）を、同じターンで同じ内容にする**（原理1）。**html の更新は子エージェントに出す**（「作業は子エージェントに出す」）
7. **子が返した突き合わせ結果を確認する。** md にあるのに画面に無い、画面にだけ古い数字が残っている、が無いこと。**「直したつもり」で終えない**
8. 評価が大きく変わったときだけ LOG に変更前後と根拠を残す

全ループが終わってから、**複数ループを合成している表示**（一覧の行・累計など）を最後に1回まとめて直す。

★ **差し替える場所をチェックリストにしない。** 置き場所は増えるし、見出しの構成も変わる。守るのは「**このループの md と画面に、前の窓の数字が1つも残っていない**」という結果だけ。判断は「**この数字は、いま取り直した窓のものか**」の1問で足りる。

★ **窓の数字は本文にも書き写されている。** ゴールの下の一文、ボトルネックの考察と施策案、施策の観測、一覧の行——**数字を文章に埋めた場所は、そのまま更新対象になる**。

### 判定を確定する

更新・相談時に判定期日を過ぎていたら声をかける。**このスキル単体には定期起動・通知機能はない。**
**予想と実際、同期間の他の変更、比較の限界を並べて示す。** AI の現在評価と、ユーザーが確定する判定を区別する（原理3）。

効いた ／ わからなかった ／ 効かなかった ／ 逆効果、の4択から推しと理由を伝え、**決めるのは本人**。
「わからなかった」は計測の穴・他の変更・母数不足などで判断できなかった結果であり、「効かなかった」ではない。
最後に来月の自分に残すひとことをもらい、同じ RECORD に確定判定・日付・理由を保持する。LOG にも確定した事実を残す。
判定後も RECORD は消さず、新しい証拠があれば現在評価を見直す。

---

## 置き場所の仕分け

原理1の適用表。迷ったらこれに当てる。

| 種類 | 置き場所 |
|---|---|
| いまの数字の内訳（最新の1窓ぶん） | **REPORT** |
| 時間とともにどう動いたか | GOAL の数字と折れ線 |
| 施策がいまどう評価されるか・その根拠 | RECORD |
| いつ何があったか | LOG（`loops/logs/<ID>.md`） |
| これからどうするか | BOTTLENECK の施策案 |
| 生データ（CSV など） | ループには置かない。md に写した値が正 |
| 仕組み・規約（窓の決め方、画面の作り） | **`SKILL.md` / `references/`。ループに書かない**（原理7） |
| プロジェクト固有の数字の取り方 | `loops/README.md` |

判断は「**これは、いまの詰まりを示しているか。それとも経過か**」の1問で足りる。

**フィードバックが来たら、まず切り分ける。「このループの中身の話」か「仕組みの話」か**（原理7）。
ループに持たせるのは**規則を当てはめた結果の値**だけ（`window: 8/29〜8/31`、`value: 37`）。**規則そのものは持たせない。**

---

## データの持ち方

**現在地・数字・施策評価の正は `loops/*.md`、出来事の正は `loops/logs/*.md`。`loops/index.html` は生成物**（原理1）。

- md を直したら、**同じターンで html も直す**（html は子エージェントに出す）
- **html だけを直さない**
- html の中身は部分的に差し替える。全文を書き直さない
- ★ **差し替えたら、タグの開閉を数えて確かめてから返す**（`references/HTML生成.md`「更新するとき」の検算）。
  `</div>` が1つ余るだけで後ろのセクションが枠の外にはみ出し、背景色が変わる。見た目では気づけない（2026-09-10 に実際にやった）
- 生成物である html に「直接編集しないでください」と書いておく
- LOG 全文をループの md や HTML に複製しない

---

## 書き方

### ★ 一目で終わりが見えない塊は読まれない

人は段落を読む前に「見る」。終わりが見えない塊は、中身を読む前に飛ばされる。**正しさは関係なく、量の時点で情報が死ぬ**（原理6）。md も画面も同じ。

- **1ブロック＝1つの事実、3行以内。先頭に結論1文。** 1段落に事実を3つ入れない
- **数字は文章に埋めない。** 表・棒・ファネル・単独の大きな数字で見せる。「9/7 9回・7回／9/8 17回・14回…」のような数列を文に書かない。誰も読まないうえ、その段落ごと飛ばされる
- **説明・計測の注意・生データ・通算は折りたたむ。** 判断に要らないものは見える場所に置かない。開けば読める、でいい
- **太字は1ブロックに1つ。** 10箇所あると、どれも強調ではなくなる
- 削除ではなく移動。**数字と事実は落とさない**。畳んだ中に全部残す

**平易な日本語で書く**（原理6）。「HPを見た」「予約した」「最後まで遊べた」のレベル。
`CVR` `リテンション` `ファネル離脱` のような語を持ち込まない。**来月これを読むのは本人で、そのとき専門語は思い出す手間になるだけ。**

数字は必ず**何の数字か**とセットで書く（原理2）。`29%` ではなく `完走率 29%（63回始まって18回終わった）`。
**数字が無いのに断定しない。**「たぶん」「未確認」はそう書く。

**ユーザーの指示なく `git commit` しない。**

---

## 参照

| ファイル | いつ読むか |
|---|---|
| `references/ファイル形式.md` | `loops/` の新規作成・md更新のとき |
| `references/HTML生成.md` | `index.html` を作る・更新するとき（画面の作りは全部こちら）。**右ペインの設置も必須手順としてここにある** |
| `references/なぜこの規約か.md` | 規約の理由を知りたいとき・規約を足したいとき |
| `assets/template.html` | html を新規に作るとき（CSSとJSをそのまま使う） |
| `assets/chat-pane.sh` | 右ペインのランチャー。プロジェクトの `loops/chat-pane.sh` に写して使う |
| `CHANGELOG.md` / `VERSION` | 版を上げるとき。互換を壊す変更は **移行が必要** と書き、「旧形式の `loops/`」節にも手順を足す |
