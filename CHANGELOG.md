# CHANGELOG

更新したら `install.sh` がこのファイルの**先頭の項**を表示します。
**「移行が必要」** と書かれた版は、既存プロジェクトの `loops/` に手が要ります。次に `/rising-loop` を使ったときにスキルが提案します。

## 1.0.1 — 2026-09-16

- 右ペインの案内（`💡`）で、起動コマンド2つの上に「Claude Code を使う人」「codex を使う人」の小見出しを付けた。右端の小さなラベルだけでは2つある意味が分からなかった
- `install.sh` を zip から tar.gz 取得に変更（macOS の unzip が日本語ファイル名で壊れる）

## 1.0.0 — 2026-09-16

初めて版を付けた版。9/9〜9/16 の変更をまとめてここに置く。

**移行が必要**（2026-09-11 より前に作った `loops/` がある人）
- 右ペインの起動を `loops/chat-pane.sh` に変えた。`index.html` にセッションID（`SESS`）を埋め込む形をやめ、`loops/.chat-sessions` に持つ。旧 `index.html` のままだと右ペインが繋がらない

**変わったこと**
- スキル名を `loop-manager` から `rising-loop` に改称。旧名でも発動する
- 7つの原理を冒頭に書き出し、全規約をその下に並べ直した
- 子ループを廃止。TRIAL は「効果を確かめる／確かめない」の2つだけに
- 右ペインを Claude Code / codex の2択に。導入時に `ttyd` `claude` `codex` の有無を必ず確認する
- 導入・ループ作成後は `file://` の URL を渡し、そこから始めてもらう
- 施策の評価に「測らない」を置かない。計測が走った時点で A〜E を付ける。評価文は120字以内、詳細は本文へ
- 「一目で終わりが見えない塊は読まれない」を規約に。結論1文＋折りたたみ、数列は表へ、太字は段落に1つ
- 実装に類する作業は子エージェントに出し、窓口は自分でコードを触らない
- `install.sh` `VERSION` `CHANGELOG.md` `README.md` を追加。配布は zip をやめ、1行コマンドに
