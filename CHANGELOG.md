# CHANGELOG

更新したら `install.sh` がこのファイルの**先頭の項**を表示します。
**「移行が必要」** と書かれた版は、既存プロジェクトの `loops/` に手が要ります。次に `/rising-loop` を使ったときにスキルが提案します。

## 1.5.1 — 2026-09-18

- 一覧のタイトル下に「主要な数字」を必ず置く（大きく1つ・内訳2〜4個・いつ時点か）。何を置くかは AI が決めて確認を取る。以前の「合算が無ければ消す」をやめた。無い殻には「合わせて」で提案

## 1.5.0 — 2026-09-18 ★ 移行が必要

- **`LXX.md` を廃止し、ループ頁 `LXX.html` を正にした。** 値の正は頁の中の `LOOP_DATA`（md の frontmatter を吸収し、`title` `summary` `updated` `metric` `trials[]` `records[]` を持つ）、画面の散文はその写し。`loops/logs/*.md`（出来事）と `loops/README.md`（数字の取り方）は md のまま
- **移行は「このループを合わせて」で提案する**（`assets/merge-md.py`。**1ループずつ**、1ループ＝1子で並列可）。判定は頁に `data-page-schema="2"` が無ければ 1.4.x 以前。直後に「更新」を1回走らせて散文を揃え、`LXX.md` は `loops/.tmp/` に**退避**する（削除はユーザー）
- **突き合わせが頁の中だけになった。** 「md ⇄ html」ではなく「散文の数字 ⇄ `LOOP_DATA`」。対象は `metric`・`hist`・`funnel` の値で、派生値（割合・倍率・差分）と RECORD の観測表は markup が正
- **`references/ファイル形式.md` を廃止。** LOG と README の規則、`loops/` のファイル一覧は `references/HTML生成.md` に移した（`LOOP_DATA` の定義と写しの規則も同じ場所）

## 1.4.0 — 2026-09-18

- 使い方の帯に「⬆ アップデート」ボタン。押して右ペインに貼ると、AI が install.sh の実行 → 規約の読み直し → 「合わせて」まで通しでやる（画面のボタンから来たときだけ AI が install.sh を実行してよい）

## 1.3.3 — 2026-09-18

- TRIAL のステータスが長いとき、施策名が1文字幅に潰れていたのを修正（ステータスを折り返し、入らなければ名前の下に）

## 1.3.2 — 2026-09-18

- install.sh の初回導入の案内を充実（とは／はじめかた／効果を上げるコツ）。初回は「変わったこと」を出さない。README も同じ内容に

## 1.3.1 — 2026-09-18 ★ 移行が必要（1.3.0 で分割した人）

- **1.3.0 の分割で、プロジェクト独自の CSS を落としていた**（プレイルームで表が縦一列に崩れた）。分割は元の `<style>` のうち `rising.css` に無い規則を `loops/project.css` に移すようにし、殻と頁は `rising.css` の後に `project.css` を読む。「合わせて」は `project.css` に触らない
- 1.3.0 で分割済みの人は「このループを合わせて」で `split-index.py --project-css` を提案する（退避ファイルから作り直す）

## 1.3.0 — 2026-09-17 ★ 移行が必要

- **画面を分けた。`loops/index.html` 1枚 → 殻（`index.html`）＋ ループ頁（`LXX.html`）＋ 共通ファイル（`rising.css` `rising.js`）。** 殻は一覧・右ペイン・使い方・モーダル・トーストと `<iframe id="loop-frame">` を持ち、ループの中身は各頁が持つ。`file://` で開けること、画面を切り替えても右ペインが切れないことは変わらない。**「このループを合わせて」でスキルが移行を提案する**（`assets/split-index.py`。元の `index.html` は `loops/.tmp/index-before-split.html` に退避）
- **数字の更新で、ループごとの子が `LXX.html` を直接書くようになった。** 断片の切り出しと差し戻しが要らなくなり、`assets/splice-section.py` は廃止
- **`assets/template.html` を廃止した。** 殻の雛形 `assets/index.html` と、ループ頁の雛形 `assets/loop.html` の2つに分けた。画面を作るときは、この2つと共通ファイル（`rising.css` `rising.js`）をコピーする
- **「このループを合わせて」は、共通ファイルを上書きして殻を入れ替えるだけになった。** `rising.css` `rising.js` `chat-pane.sh` は `assets/` のものをそのままコピー、殻は `assets/shell-update.py`（CONST と LOOPS ブロックだけを引き継ぎ、共通ファイル3つも同時に上書きする）。**ループ頁は触らない**（頁の `data-page-schema` が上がった版だけ、その版の手順で直す）
- install.sh: 別の場所に古い rising-loop（旧名 `loop-manager` を含む）が残っていると、削除するよう案内する
- 対象を macOS と明記。Windows は WSL の中で使う（install.sh は PowerShell では止まる）

## 1.2.8 — 2026-09-17

- **数字の更新を速くした**（6ループで 31分 → 6〜7分の見込み）。親が数字取りを1回だけ実行して配り、ループごとの子を1メッセージで同時に起動。子は md と画面の断片（`loops/.tmp/s-LXX.html`）だけを直し、親が `assets/splice-section.py` で index.html に差し戻す。子にスキル本文を読ませない
- md の frontmatter に `summary:`（一覧の一文）を追加。一覧はこれを写すだけ。「このループを合わせて」で既存の md にも足す
- `loops/.tmp/` は作業用。正ではない

## 1.2.7 — 2026-09-17

- 右ペインの Claude を `--autocompact 200000` で起動。文脈が20万トークンを超えたら自動で要約し、更新が遅くなるのを防ぐ。「このループを合わせて」で `loops/chat-pane.sh` が写し直され、ペインを立て直したときから効く

## 1.2.6 — 2026-09-17

- install.sh: すでに最新版のときも、更新後に貼る1行を表示する

## 1.2.5 — 2026-09-17

- 更新後に貼る文を「rising-loop が X.Y.Z になったので、このループを合わせて」に
- 施策案の定型文「これをやって」を「TRIAL に移す」（本文「これを TRIAL に移して手順を出して（実装はしない）」）に。「これをやって」だと実装まで走ることがあった

## 1.2.4 — 2026-09-17

- 「現行ループを合わせて」は最初に `/rising-loop` を読み直し、帯の版がスキルと同じなら読み直しだけで終わる。開いている各ループの右ペインでも同じ文を送れば、古い規約が上書きされる
- 画面から貼る指示文の `rule:` を「まず /rising-loop を呼び出して最新の手順を読み、それに従うこと」に

## 1.2.3 — 2026-09-17

- 使い方の帯の右に RISING LOOP・版・Created by Voice App Lab を表示。版は「合わせて」のたびに書き換える

## 1.2.2 — 2026-09-17

- 「現行ループを合わせて」で、既にある共通部品（札・トースト・使い方など）の CSS/JS も template のものに差し替えるよう明記。1.2.0 では足りない部品を足すだけで、札の文字サイズが旧のまま残っていた

## 1.2.1 — 2026-09-17

- 右上の「? 使い方」と「AI ◂」の間が空きすぎていたのを修正（2つを1つの枠に並べる）

## 1.2.0 — 2026-09-17

- 導入直後の案内: 材料を入れたフォルダで Claude Code（Codex）を起動し `/rising-loop` と打つ、を install.sh と README に
- 更新後の案内: 「rising-loop が X.Y.Z になったので、現行ループを合わせて」と伝えれば、スキルが md の形式と画面の部品を最新に合わせる
- 一覧に「ループを追加」ボタン。押すと目標をひとことで書いて送れる（札に例が3つ）
- 初回起動（`loops/` が無いとき）は目標の例を10個出して選ばせる。一覧が0件の画面にも同じ10個を薄く並べる

## 1.1.3 — 2026-09-17

- 使い方の見出しをパネル上端のベタ塗り帯（白抜き）に

## 1.1.2 — 2026-09-17

- 使い方の見出しを「ライジング・ループの使い方」に。冒頭にひとことの説明（ひとつの指標を計測し、その数字を上げる施策を AI と共に進めるループシステム）

## 1.1.1 — 2026-09-17

- `install.sh` がバックアップを取らなくなった。`rising-loop.bak-…` を同じ階層に残すと別スキルとして登録されてしまうため。残っている `.bak-*` は次の実行で削除する

## 1.1.0 — 2026-09-17

初めての人が「施策は出たが、何をやっていいか分からない」で止まっていたのを直した。

**移行が必要**（1.1.0 より前に作った `loops/` がある人。次に `/rising-loop` を使ったときにスキルが提案する）
- md の `trial:`（1件）が `trials:`（配列・何件でも）になった
- TRIAL と RECORD に同じ施策がある場合は、1件ずつ「完了にしますか」と聞いてどちらかに寄せる

- 画面に **`? 使い方`** を追加（初回は自動で開く）。図＋手順3つ＋「右のチャットはただの Claude Code（Codex）です」
- 指示のモーダルに定型の札「これをやって」「これは消して」「この認識は違う」
- コピーしたら右ペインが開き、トーストが矢印で貼り先を指す。トーストは押すと閉じる（押した先で下のボタンが反応するバグを修正）
- 右ペインが開いているとき、モーダルがペインの下に潜らない
- ボタンを 📋 付きの破線の札にし、「押しても実行されない」と見た目で伝える
- 見出し「トライ中の施策」を「施策の実行」に（図の4段階と同じ言葉に揃えた）
- 規約: 導入・現在地報告の締めは「まず施策を1つ選んで、『指示する』から『これをやって』と送ってください」
- **施策の進み方を変えた**（実行 → 評価の移行が分かりにくかった）
  - 施策案を拾ったとき、効果の数字は聞かない。代わりに実現手順のチェックリスト（⬜）をその場で TRIAL に出す。全部 ✅ で実施完了
  - 実施が終わっても TRIAL に止まる。RECORD を先に作らない（同じ施策が2か所に見えていたのをやめた）
  - TRIAL の「指示する」に札「完了にして（評価に移す）」。これがユーザー起点の唯一の入口
  - 「完了にして」のときに初めて「効果を数字で確かめますか」→ 見る数字・判定日・予想を聞き、RECORD に移す
  - TRIAL は何件でも並べられる（「1ループに1つ」と「待つ／入れ替える」の二択を廃止）
  - 予想を聞くのは「完了にして」のときだけ。それより前に AI から数字や測定の話を持ちかけない

既存プロジェクトの `index.html` には、`/rising-loop` で「使い方を追加して」と頼めば `assets/template.html` から写す（移行は不要）。

## 1.0.1 — 2026-09-16

- 右ペインの案内（`💡`）で、起動コマンド2つの上に「Claude Code を使う人」「codex を使う人」の小見出しを付けた。右端の小さなラベルだけでは2つある意味が分からなかった
- `install.sh` を zip から tar.gz 取得に変更（macOS の unzip が日本語ファイル名で壊れる）

## 1.0.0 — 2026-09-16

初めて版を付けた版。9/9〜9/16 の変更をまとめてここに置く。

**移行が必要**（2026-09-11 より前に作った `loops/` がある人）
- 右ペインの起動を `loops/chat-pane.sh` に変えた。`index.html` にセッションID（`SESS`）を埋め込む形をやめ、`loops/.chat-sessions` に持つ。旧 `index.html` のままだと右ペインが繋がらない

**変わったこと**
- スキル名を `loop-manager` から `rising-loop` に改称。旧名でも発動する
- 7つの原理を冒頭に書き出し、全規約をその下に並べ直した
- 子ループを廃止。TRIAL は「効果を確かめる／確かめない」の2つだけに
- 右ペインを Claude Code / codex の2択に。導入時に `ttyd` `claude` `codex` の有無を必ず確認する
- 導入・ループ作成後は `file://` の URL を渡し、そこから始めてもらう
- 施策の評価に「測らない」を置かない。計測が走った時点で A〜E を付ける。評価文は120字以内、詳細は本文へ
- 「一目で終わりが見えない塊は読まれない」を規約に。結論1文＋折りたたみ、数列は表へ、太字は段落に1つ
- 実装に類する作業は子エージェントに出し、窓口は自分でコードを触らない
- `install.sh` `VERSION` `CHANGELOG.md` `README.md` を追加。配布は zip をやめ、1行コマンドに
